import React from 'react';
import ReactDOM from 'react-dom/client';

import { BaseApp } from './base/app/components';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
    <BaseApp />
);
